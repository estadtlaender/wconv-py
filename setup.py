from setuptools import setup, find_packages

setup(
    name='WeakConvexity',
    version='0.0.1',
    description='A small library implementing weakly convex hull operators',
    packages=find_packages()
)
